---
# MINIRHIS Ansible Inventory (YAML)
# Auto-generated by MiniRHIS.sh (generate_rhis_inventory) from
# ~/.ansible/conf/env.yml.  Do NOT commit resolved host details here.
#
# Use with:
#   ansible-inventory -i container/vars/external_inventory/hosts.yml --graph
#
# All hostname and IP values below are placeholders.
# Run  ./MiniRHIS.sh --reconfigure  then
#      ./MiniRHIS.sh --generate-inventory  to produce the live version.

all:
  vars:
    ansible_ssh_common_args: >-
      -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o
      ForwardX11=no

  children:
    ansibledev:
      hosts:
        "kaso.prod.spg": {}

    libvirt:
      hosts:
        "kaso.prod.spg": {}

    installer:
      hosts:
        "kaso.prod.spg":
          ansible_host: "192.168.122.1"
          ansible_user: "admin"
          ansible_become: true

    scenario_satellite:
      hosts:
        "satellite.prod.spg":
          ansible_host: "10.168.128.1"
          ansible_user: "admin"
          ansible_become: true

    sat_primary:
      children:
        scenario_satellite: {}

    aap:
      hosts:
        "aap.prod.spg":
          ansible_host: "10.168.128.2"
          ansible_user: "admin"
          ansible_become: true

    aap_hosts:
      children:
        aap: {}

    platform_installer:
      children:
        aap: {}

    idm:
      hosts:
        "idm.prod.spg":
          ansible_host: "10.168.128.3"
          ansible_user: "admin"
          ansible_become: true

    idm_primary:
      children:
        idm: {}
