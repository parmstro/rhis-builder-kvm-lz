---
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    infra_servers:
      hosts:
        infra1:
          ansible_host: 10.10.10.51
          idrac_ip: 10.5.0.11
          idrac_user: "sdp"
          # In a real environment, consider using Ansible Vault for the password
          idrac_password: "EXAMPLE_PASSWORD"
        infra2:
          ansible_host: 10.10.10.52
          idrac_ip: 10.5.0.12
          idrac_user: "sdp"
          # In a real environment, consider using Ansible Vault for the password
          idrac_password: "EXAMPLE_PASSWORD"
        infra3:
          ansible_host: 10.10.10.53
          idrac_ip: 10.5.0.13
          idrac_user: "sdp"
          # In a real environment, consider using Ansible Vault for the password
          idrac_password: "EXAMPLE_PASSWORD"
    http_servers:
      hosts:
        web1:
          ansible_host: 10.5.20.20
...
