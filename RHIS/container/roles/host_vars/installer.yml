# installer.yml — generated by run_minirhis_install_sequence.sh
ansible_user: "{{ installer_user }}"
aap_remote_user: "{{ admin_user | default('admin') }}"
ansible_ssh_private_key_file: "{{ minirhis_installer_ssh_private_key_file | default('/home/sgallego/.ssh/minirhis-installer/id_rsa') }}"
