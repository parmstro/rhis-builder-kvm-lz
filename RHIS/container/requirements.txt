---
# Python requirements for the MiniRHIS Podman provisioner container runtime.
# Install with:  pip install -r container/requirements.txt
# For the full developer environment, see requirements.txt in the
# repository root.

requests>=2.28.0
jinja2>=3.0.0
PyYAML>=6.0
paramiko>=2.12.0
netaddr>=0.8.0
boto3>=1.26.0
botocore>=1.29.0
dnspython>=2.2.0
cryptography>=38.0.0
